<body class="container" style="padding: 5px;">
    <div style="padding:20px;">
        <div class="intro" style="background:linear-gradient(to right,purple,orange,purple,orange,purple,navy,purple,navy);
        color:aliceblue;border-radius:7px;padding:20px">
            <div> 
            <h1>It's Popping!</h1>
            </div>
            <div class="vibeImg">   
                <img src="bc3.gif"   alt="dance-image" style="width:250px;"/>
            </div>
        </div>
       <div class="row" style="margin-top: 50px;">
        {{#each featured}}
            <div class="featured col-sm" style="margin-bottom: 10px;margin-right:10px">
                <h3>{{this.name}}</h3>
                <h4>{{this.fee}}</h4>
                <p>{{this.description}}</p>
            </div>
        {{/each}}
      </div>
    </div>
</body>